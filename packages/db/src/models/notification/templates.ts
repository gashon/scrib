import login from '@scrib/api/templates/emails/login';
import Template from '@scrib/api/templates/types/template';

const templates: { [key: string]: Template } = {
  login,
};

export default templates;
